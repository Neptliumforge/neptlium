# Browser-safe variables: none currently required.
# Add only documented NEXT_PUBLIC_ variables when the application consumes them.
# Server-only provider credentials are not required for this frontend.
