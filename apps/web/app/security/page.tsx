import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { SecurityFlowVisual } from '@/components/product-visuals';
import { createPageMetadata } from '@/lib/seo';
export const metadata=createPageMetadata({title:'Security — System and Authority Boundaries | Neptlium',description:'Neptlium Security describes supported boundaries around authentication, authorization, privileged operations, financial state, provider evidence and fail-closed behavior without implying undocumented certifications.',path:'/security'});
const boundaries=[['Authentication','Identity establishes who is present. It does not by itself establish ownership, permission or authority for a consequential operation.'],['Authorization','Roles, ownership, policy and resource state remain server-enforced considerations before sensitive work may proceed.'],['Client boundary','Public browser presentation is not the financial control plane. Provider secrets, service credentials and privileged commands belong outside client authority.'],['Service boundary','Canonical financial and account authority belongs in the responsible server-side service rather than being reconstructed from marketing or browser state.'],['Evidence','Consequential transitions should be supported by the evidence appropriate to their lifecycle state rather than inferred from presentation or intent.']] as const;
export default function SecurityPage(){return <div className="security-architecture-page"><section className="security-hero architecture-dark"><div className="web-shell security-hero-grid"><div><p className="web-eyebrow">Security</p><h1>Security begins with explicit system and authority boundaries.</h1></div><div><p>Neptlium separates authentication, authorization, privileged operations, canonical financial state and provider evidence because each carries a different security responsibility.</p><p>This page describes supported architectural principles. It does not claim certifications, insurance, regulatory approval, penetration-test status, specific cryptographic controls or perfect security unless those facts are separately documented.</p></div></div></section><section className="security-control-map" aria-labelledby="boundaries-title"><div className="web-shell security-control-grid"><div><p className="web-eyebrow on-light">Control boundaries</p><h2 id="boundaries-title">A consequential action should cross the boundaries that its risk requires.</h2><div className="product-story-visual security-primary-visual"><SecurityFlowVisual /></div></div><div className="security-boundary-list">{boundaries.map(([title,body],index)=><article key={title}><span>{String(index+1).padStart(2,'0')}</span><div><h3>{title}</h3><p>{body}</p></div></article>)}</div></div></section><section className="security-principles"><div className="web-shell security-principles-grid"><article><h2>Fail closed</h2><p>Missing credentials, unavailable dependencies or unverified capability must not be represented as simulated success.</p></article><article><h2>Replay resistance</h2><p>Consequential operations are designed to resist accidental duplication and unsafe replay where those workflows are implemented.</p></article><article><h2>Data boundaries</h2><p>Ownership, row-level controls and service boundaries constrain access according to the responsible subsystem where those controls are implemented.</p></article></div></section><section className="security-close architecture-dark"><div className="web-shell product-story-close-grid"><h2>Security describes supported control boundaries. Trust describes the standard for representing them.</h2><Link className="text-arrow-link on-dark" href="/trust">Continue to Trust <ArrowRight aria-hidden="true" /></Link></div></section></div>}
