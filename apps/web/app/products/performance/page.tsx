import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { createPageMetadata } from '@/lib/seo';
export const metadata=createPageMetadata({title:'Performance — Outcome Interpretation | Neptlium',description:'Neptlium Performance frames portfolio outcomes with source, period, methodology and cash-flow context while distinguishing calculated, provider-reported and modeled information.',path:'/products/performance'});
const questions=[['Source','Which evidence defines the portfolio scope and whether the result is provider-reported, calculated or otherwise derived?'],['Period','What exact interval does the result describe, and are comparison periods defined consistently?'],['Method','Which calculation method, assumptions and data limitations affect interpretation?'],['Cash flows','Which deposits, withdrawals or transfers changed capital during the period and must be separated from investment outcome?'],['Context','How do composition, concentration and liquidity affect the meaning of the result?']] as const;
export default function PerformancePage(){return <div className="product-story performance-story"><section className="performance-hero"><div className="web-shell performance-hero-inner"><p className="web-eyebrow on-light">Products · Performance</p><h1>Performance requires context before interpretation.</h1><p>A return figure is not self-explanatory. Source evidence, period, methodology, cash flows and portfolio structure determine what an outcome can responsibly say.</p><p>Neptlium is designed to keep calculated, provider-reported and modeled information distinguishable. The public site presents no customer returns, performance guarantees or investment outcomes.</p></div></section><section className="performance-evidence" aria-labelledby="evidence-title"><div className="web-shell"><div className="performance-evidence-heading"><span>Interpretation framework</span><h2 id="evidence-title">Five questions before reading an outcome.</h2></div><dl className="performance-question-list">{questions.map(([term,description],index)=><div key={term}><dt><span>{String(index+1).padStart(2,'0')}</span>{term}</dt><dd>{description}</dd></div>)}</dl></div></section><section className="performance-boundary architecture-dark"><div className="web-shell performance-boundary-grid"><div><p className="web-eyebrow">State boundary</p><h2>Calculated, provider-reported and modeled results do not carry the same authority.</h2></div><div><p>Where performance information becomes supported, its provenance and methodology should travel with it. A derived estimate should not inherit the authority of provider evidence merely because both can be displayed in the same format.</p><p>Performance interpretation is informational context, not a guarantee, forecast or investment recommendation.</p></div></div></section><section className="performance-return-context"><div className="web-shell performance-return-grid"><div><h2>Return the outcome to the portfolio that gives it meaning.</h2><p>Performance belongs beside composition, exposure, concentration, liquidity and capital activity. The operating model is designed to connect those relationships rather than isolate a percentage from the state that produced it.</p></div><Link className="web-button primary" href="/products/portfolio-intelligence">Portfolio Intelligence <ArrowRight aria-hidden="true" /></Link></div></section></div>}
