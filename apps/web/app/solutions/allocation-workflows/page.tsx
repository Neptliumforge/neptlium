import { EditorialMarketingPage } from '@/components/editorial-marketing-page';
import { createPageMetadata } from '@/lib/seo';
export const metadata=createPageMetadata({title:'Allocation Workflows — Neptlium',description:'Turn a view into an intentional decision process.',path:'/solutions/allocation-workflows'});
export default function Page(){return <EditorialMarketingPage eyebrow="Allocation workflows" title="Turn a view into an intentional decision process." lead="Carry capital intent through modeling, review and authorization without collapsing proposed state into financial consequence." chapters={[{title:'Start with current state.',body:'Understand the portfolio as it is before introducing a target, recommendation or policy response.'},{title:'Model the intended change.',body:'Express targets, constraints and trade-offs as modeled state so everyone can reason about the proposal before it becomes consequential.'},{title:'Govern the transition.',body:'Review and authorization remain explicit so the workflow shows exactly when intent becomes permission.'}]} cta={{label:'Explore Allocation',href:'/products/allocation'}}/>}
