# Provider Doctrine Scope Lock

This slice is complete when its architecture primitives and documentation validate. Do not expand it into production credential changes, provider dashboard mutations, live transactions, database migrations, UI redesign, unrelated Treasury functionality or broad repository cleanup.

The only primary provider roles established here are Circle, Alchemy and Stripe. Supabase remains authentication/persistence infrastructure under existing authority and is not reclassified as a money/chain provider.

Further runtime integration belongs to the next focused execution after overlap reconciliation.
