# Shared browser-safe identity/session variables used by authenticated frontends.
# Workspace-specific routing and all server/provider secrets belong in workspace examples.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# CONFIGURED != CAPABILITY VERIFIED.
# SOURCE SUPPORT != LIVE AVAILABILITY.
