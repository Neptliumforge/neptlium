# Authentication is configured per authenticated workspace and uses Supabase Auth only.
# Browser-safe Supabase public configuration belongs in app-specific examples;
# service-role and other server secrets never belong in this root file.

# CONFIGURED != CAPABILITY VERIFIED.
# SOURCE SUPPORT != LIVE AVAILABILITY.
