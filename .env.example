# Authentication is configured per authenticated workspace and is Clerk-only.
# Browser-safe Clerk publishable keys belong in apps/app or apps/admin examples;
# server secrets never belong in this root file.

# CONFIGURED != CAPABILITY VERIFIED.
# SOURCE SUPPORT != LIVE AVAILABILITY.
