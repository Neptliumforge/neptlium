# pnpm 11 reads project .npmrc files for registry/auth settings only.
# No project-scoped registry override or credential is currently required.
# pnpm-specific repository policy belongs in pnpm-workspace.yaml.
